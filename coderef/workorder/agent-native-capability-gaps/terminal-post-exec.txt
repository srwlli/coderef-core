================================================================================
WORKORDER PHASE EXECUTION COMPLETE (strict-phased — HARD STOP)

WORKORDER ID: WO-AGENT-NATIVE-CAPABILITY-GAPS-001
PHASE COMPLETED: Phase 6 of 6 — Promote Go to a real call+import+export graph (HEAVY)
MODE: --phase=6 single-phase (per-phase hard stop; FINAL phase — WO now close-ready)

DELIVERABLE:
  Go is no longer scan-only/graph-dead. Three raw-fact emitters in
  relationship-extractor.ts feed the language-agnostic resolver, producing
  resolved cross-file call edges, import edges, and export edges for .go files
  — proving the graph-dead-language promotion path works end-to-end.

  Components:
    - walkRawGoImports: import_declaration -> import_spec (field path =
      interpreted_string_literal, quotes stripped; optional alias field name ->
      namespaceImport unless _ or .). Emits RawImportFact.
    - walkRawGoCalls: call_expression where field function is a bare identifier
      OR a selector_expression (fields operand=receiver / field=callee), with
      scopePath tracking through function_declaration/method_declaration.
      Emits RawCallFact.
    - walkRawGoExports: Go visibility model — capitalized top-level names
      (/^[A-Z]/) on function_declaration, method_declaration, and
      type_declaration->type_spec are named exports. Emits RawExportFact.
    - Dispatch: case 'go' added to extractRawImports/extractRawCalls/
      extractRawExports (Go removed from default:break). Resolver UNTOUCHED —
      the language-family guard (STUB-M3GE4S) already isolates family 'go'
      from js-ts and python; nothing can cross-bind.

TASK CHECKLIST:
  [x] P6-T1 READ    relationship-extractor dispatch + TS/JS emitter templates;
                    learned authoritative Go AST node/field names by PARSING
                    (import_spec path/name fields; selector_expression
                    operand/field; method name = field_identifier)
  [x] P6-T2 WRITE   walkRawGo{Imports,Calls,Exports} emitters
  [x] P6-T3 EDIT    route case 'go' through all 3 dispatch switches
  [x] P6-T4 TEST    __tests__/pipeline/go-graph-ground-truth.test.ts — 5 tests
  [x] P6-T5 RUN     build exit 0; suite 1549 passed | 26 skipped (+5);
                    live populate CLI on real Go project; self-scan anchor
  [x] P6-T6 COMMIT  ba8fec7 by explicit path (main-only)
  [x] P6-T7 STOP    this terminal surface

COMMITS:
  - ba8fec7: feat(agent-native-capability-gaps): phase 6 - promote Go to a real
             call+import+export graph (HEAVY)

TEST RESULTS:
  - Build (tsc + tsc -p tsconfig.cli.json): clean, exit 0
  - vitest run: 1549 passed | 26 skipped (161 files) — exit 0
  - NEW go-graph-ground-truth.test.ts: 5 passed
      1. cross-file call resolves to node-id endpoints (Run -> Helper)
      2. import + export edges emitted (fmt import; Run/Widget exports)
      3. unknown stdlib call (fmt.Println) = explicit unresolved fact (RISK-03)
      4. duplicate top-level name = ambiguous with BOTH candidates
         (live-probed FIRST, then asserted — a.go/b.go Helper)
      5. no symbol leak across the language-family guard

LIVE PROOF (real populate CLI on a real Go project):
  - graph.json persisted: 4 nodes / 5 edges
  - Helper call RESOLVED cross-file: @Fn/main.go#Run:3 -> @Fn/util.go#Helper:2
  - fmt.Println: unresolved (never guessed)
  - import "fmt": explicit unresolved edge with originSpecifier
  - exports: @Fn/main.go#Run:3, @Fn/util.go#Helper:2 (capitalized = exported)

REALITY ANCHOR (AC-6 + AC-7):
  - Repo contains ZERO .go files -> anchor must be attributable-stable, and is:
    self-scan 2446 elements / 30,112 edges (P5: 2437 / 29,984). Deltas are
    EXACTLY the P6-authored code scanning itself: 3 walkRawGo* walker nodes in
    relationship-extractor.ts + 5 nodes from the new test file; 0 nodes from
    .go files. Validation counts moved only by the new source lines
    (valid 5280, unresolved_src 5522, ambiguous_src 1186).
  - TS/JS extraction behavior unperturbed: all 1544 pre-existing tests green.
  - .coderef/ stays gitignored. No cloud provider. Main-only by explicit path.

ACCEPTANCE CRITERIA (P6 scope):
  [PASS] AC-6: Go produces resolved cross-file call edges + import/export edges
               via raw-fact emitters through the UNCHANGED resolver; unknown
               calls stay explicit unresolved facts; duplicates ambiguous with
               candidates (resolver discipline holds — nothing guessed).
  [PASS] AC-7 (P6 portion): existing MCP tools + shapes unchanged; full vitest
               green; no cloud provider; every commit main-only by explicit path.

HARD STOP: Phase 6 complete — ALL 6 PHASES COMPLETE (P1 e5b6f66, P2 0ecb4f9,
           P3 1ff9a76, P4 106271b, P5 4a8c781, P6 ba8fec7 — all PASS).
WO STATUS: WO-AGENT-NATIVE-CAPABILITY-GAPS-001 is COMPLETE and close-ready.
NEXT: /close-workorder WO-AGENT-NATIVE-CAPABILITY-GAPS-001   (operator-invoked;
      never auto-closed). Deferred follow-ups live in plan.json
      out_of_scope_followups (cross-repo resolution, scored graph-eval,
      rust/java/cpp promotion) + REC-P1-004/005.
================================================================================
