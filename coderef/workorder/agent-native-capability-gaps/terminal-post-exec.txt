================================================================================
WORKORDER PHASE EXECUTION COMPLETE (strict-phased — HARD STOP)

WORKORDER ID: WO-AGENT-NATIVE-CAPABILITY-GAPS-001
PHASE COMPLETED: Phase 5 of 6 — Graph-safe incremental watch (HEAVY)
MODE: --phase=5 single-phase (per-phase hard stop; operator go required for P6)

DELIVERABLE:
  Graph-safe incremental populate — re-resolve ONLY changed files WITHOUT losing
  cross-file resolution against unchanged files. Closes the corruption trap the
  hard_constraint warns about.

  ADJ-03 (operator-ruled): P5-T1 READ proved byte-identical parity needs resolve+
  construct over the FULL merged fact universe, not just changed-file calls
  against a symbol table — resolveImports needs ALL files' rawExports and
  constructGraph emits edges for the whole element set. The orchestrator's
  existing incremental path filters `files` to changed-only and NEVER merges
  unchanged facts back (allFilesUnchanged feeds only the hit-ratio metric) —
  THAT is the trap. Surfaced two architectures as a halt; operator ruled the
  full-fact-set approach (parity is STRUCTURAL: same pure-function inputs ->
  byte-identical output; reuses the tested resolve/construct chain unchanged).

  Components:
    - src/pipeline/symbol-table-cache.ts (NEW): persist the complete per-file
      fact bundle to .coderef/incremental-facts.json after a full build; merge
      changed/deleted files into it. Versioned, atomic temp+rename, plain-JSON.
    - orchestrator: capture per-file bundles + order on a full run and persist
      the fact set (additive — full-build output unchanged). runIncremental()
      re-scans only changed files, SWAPS their facts into the cached full set,
      and runs the SAME resolveImports/resolveCalls/constructGraph over the
      merged universe (assembleAndResolve). Falls back to full when no fact set.
    - populate: --changed-files / --deleted-files route to runIncremental.
    - coderef-watch: opt-in --incremental passes the debounced snapshot to
      populate --changed-files (default OFF; full pipeline stays the safe path).
    - IncrementalCache drift logic UNTOUCHED (RISK-05).

TASK CHECKLIST:
  [x] P5-T1 READ    watch full-populate-every-flush; orchestrator incremental
                    filter (the trap); buildSymbolTable shape; incremental-cache;
                    prior watcher WOs -> HALT + operator ruling (ADJ-03)
  [x] P5-T2 WRITE   symbol-table-cache.ts (persisted full fact set + merge)
  [x] P5-T3 EDIT    orchestrator.runIncremental + populate/watch wiring
  [x] P5-T4 TEST    +3 parity tests (the CORRECTNESS GATE, RISK-02)
  [x] P5-T5 RUN     build exit 0; full suite 1544 passed | 26 skipped (+3);
                    live CLI parity confirmed end-to-end
  [x] P5-T6 COMMIT  4a8c781 by explicit path (main-only)
  [x] P5-T7 STOP    this terminal surface

COMMITS:
  - 4a8c781: feat(agent-native-capability-gaps): phase 5 - graph-safe incremental populate (HEAVY)

PUSH:
  - 76244f1..<pending> -> origin/main (after this artifact commit)

TEST RESULTS:
  - Build (tsc + tsc -p tsconfig.cli.json): clean, exit 0
  - vitest run: 1544 passed | 26 skipped (160 files) — exit 0
  - NEW __tests__/pipeline/incremental-parity.test.ts: 3 passed

CORRECTNESS GATE (RISK-02 — the phase's reason for existing):
  - A single-file edit via runIncremental yields a resolved-edge set (AND the
    full edge signature incl. non-resolved reasons) BYTE-IDENTICAL to a full
    rebuild — including a NEW cross-file call (extra() -> util.helper) into an
    UNCHANGED file. Deleted-file drop + no-fact-set fallback also covered.
  - LIVE CLI PARITY end-to-end through the watcher path: full populate persists
    incremental-facts.json; `populate --changed-files main.ts` (re-scan only the
    edited file) === full rebuild of the edited state; cross-file call resolved.

REALITY ANCHOR (AC-5 + AC-7):
  - Self-scan graph intact + queryable after P5: 2437 elements / 29,984 edges;
    validation counts stable (resolved 5258, unresolved 17145, ambiguous 2339).
  - Additive fact-set capture did NOT perturb any of the 1541 pre-existing tests.
  - .coderef/ (incl. incremental-facts.json) is gitignored. No cloud provider.
    Every commit main-only by explicit path.

ACCEPTANCE CRITERIA (P5 scope):
  [PASS] AC-5: incremental watch re-resolves only changed files against a
               persisted full symbol/fact table, PROVEN byte-identical to a full
               rebuild's resolved-edge set (no cross-file corruption).
  [PASS] AC-7 (P5 portion): existing MCP tools + shapes unchanged; full vitest
               green; no cloud provider; every commit main-only by explicit path.

HARD STOP: Phase 5 complete — awaiting operator go for the final HEAVY Phase 6
           (Promote Go to a real call+import+export graph via raw-fact emitters).
REMAINING: P6 (Go graph promotion — HEAVY). After P6: WO ready for /close-workorder.
NEXT: /execute-workorder WO-AGENT-NATIVE-CAPABILITY-GAPS-001 --phase=6
================================================================================
