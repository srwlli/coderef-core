================================================================================
WORKORDER PHASE EXECUTION COMPLETE (strict-phased — HARD STOP)

WORKORDER ID: WO-AGENT-NATIVE-CAPABILITY-GAPS-001
PHASE COMPLETED: Phase 4 of 6 — Build-if-missing (zero-setup for agents)
MODE: --phase=4 single-phase (per-phase hard stop; operator go required for HEAVY P5)

DELIVERABLE:
  The MCP server no longer THROWS when .coderef/ is absent or stale. loadGraph/
  loadIndex now call ensureArtifacts, which builds the index on demand:
    - absent graph.json/index.json -> spawn the populate CLI in-process ONCE,
      then read the artifacts it wrote.
    - stale (any source file newer than graph.json) -> rebuild (bounded).
    - fresh, pre-built .coderef/ -> served as-is, NO rebuild (additive no-op).

  ADJ-02 (operator-ruled): P4-T1 READ found the plan premise incomplete —
  orchestrator.run() returns in-memory PipelineState but does NOT persist
  graph.json/index.json (populate.ts does that via a separate generators+
  validation+index-rewrite dance). Surfaced two paths as a halt; operator ruled
  SPAWN populate CLI (reuses the exact tested persist path, zero drift, matches
  the server's existing spawnSync(git) pattern) over in-process generators.

  RISK-04 bounds:
    - >4000 source files -> 'run populate first' hint (never a hang / long build).
    - 10-minute wall-clock timeout on the spawned build.
    - build-once guard prevents rebuild loops.
    - BuildHintError carries an actionable message when auto-build can't proceed
      (bin missing, over ceiling, build failed/incomplete).

TASK CHECKLIST:
  [x] P4-T1 READ    loadGraph/loadIndex throws-if-missing; orchestrator.run does
                    NOT persist; populate.ts is the real persist path -> HALT +
                    operator ruling (ADJ-02)
  [x] P4-T2 EDIT    ensureArtifacts build-if-missing via spawn populate
  [x] P4-T3 EDIT    bounded staleness (source-mtime vs graph.json; file ceiling)
  [x] P4-T4 TEST    +4 tests in a NEW file, real temp repo + real populate spawn:
                    absent->builds+answers, stale->rebuilds, fresh->no-op
  [x] P4-T5 RUN     build exit 0; full suite 1541 passed | 26 skipped (+4)
  [x] P4-T6 COMMIT  106271b by explicit path (main-only)
  [x] P4-T7 STOP    this terminal surface

COMMITS:
  - 106271b: feat(agent-native-capability-gaps): phase 4 - build-if-missing (zero-setup for agents)

PUSH:
  - 868d26b..<pending> -> origin/main (after this artifact commit)

TEST RESULTS:
  - Build (tsc + tsc -p tsconfig.cli.json): clean, exit 0
  - vitest run: 1541 passed | 26 skipped (159 files) — exit 0
  - New file mcp-server-build-if-missing.test.ts: 4 passed (real populate spawns)
  - mcp-server.test.ts: 65 passed (fixture marked fresh so build-if-missing
    serves it as-is — the staleness feature was correctly firing on it)

REALITY ANCHOR (AC-4 + AC-7):
  - Live: build-if-missing PROVED itself on the self-scan. The on-disk index had
    gone stale (I edited coderef-mcp-server.ts across P1-P4 with no rescan); the
    server detected staleness and rebuilt in-process, refreshing 2405 -> 2417
    elements / 29,802 edges. This SELF-HEALED the stale-index caveat flagged at P3.
  - Live: a SECOND read against the now-fresh index did NOT rebuild (mtime
    unchanged) — the pre-built-fresh path is a clean no-op.
  - The existing 18 tools + response shapes are unchanged.
  - .coderef/ is gitignored (the live rebuild is not committed). No cloud
    provider introduced. Every commit main-only by explicit path.
  - A real findPopulateBin() off-by-one (path resolution under vitest) was caught
    by P4-T4 and fixed before commit — the reason the test exists.

ACCEPTANCE CRITERIA (P4 scope):
  [PASS] AC-4: the MCP server builds .coderef/ on demand when absent/stale
               (bounded), instead of throwing — zero-setup for a fresh repo.
  [PASS] AC-7 (P4 portion): existing 18 tools + shapes unchanged; full vitest
               green; pre-built path unchanged; no cloud provider; every commit
               main-only by explicit path.

HARD STOP: Phase 4 complete — awaiting operator go for the HEAVY Phase 5
           (Graph-safe incremental watch — persisted full symbol table, delta
           re-resolve; the CORRUPTION-TRAP phase — see hard_constraints).
REMAINING: P5 (HEAVY), P6 (Go graph promotion — HEAVY).
NEXT: /execute-workorder WO-AGENT-NATIVE-CAPABILITY-GAPS-001 --phase=5
================================================================================
