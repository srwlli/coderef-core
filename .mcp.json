{
  "_comment": "RETIRED 2026-08-04: the `coderef-core` MCP server was removed from this manifest, completing the deregistration that core's issues.md LOG-001 already recorded. That entry removed it at USER scope (`claude mcp remove coderef-core -s user`) but this PROJECT-scoped manifest kept re-registering it for any session rooted here — so the record said retired while the tools stayed reachable. The gate evidence stands as LOG-001 states it: coderef-next/core's suite green, rules/drift/parity/dogfood GREEN, bound surface live-smoked, MCP leg dogfooded; and as of 2026-08-04 the 16 ASSISTANT CORE skills that fronted this server are retired too, so it has no remaining consumer.",
  "_comment_core": "`core` (coderef-next/core) is the ONE registered code-intelligence server, and is repo-agnostic: every call passes project_root explicitly, so it analyzes THIS repo as readily as any other. Nothing was lost by the removal above. ROLLBACK, if ever needed: claude mcp add coderef-core -s user -- node C:/Users/willh/Desktop/CODEREF/CODEREF-CORE/dist/src/cli/coderef-mcp-server.js",
  "mcpServers": {
    "core": {
      "command": "node",
      "args": [
        "C:/Users/willh/Desktop/coderef-next/core/packages/transports/mcp/dist/index.js"
      ]
    }
  }
}
