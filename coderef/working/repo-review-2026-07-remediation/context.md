# STUB-W45PTN — Explicit Context: Repo Review Remediation (2026-07-02)

**Source report:** `coderef/reference/repo-review-2026-07-02.md` (full review with all evidence).
**Evidence base:** fresh coderef-scan (6,249 elements / 343 files / 71.9s), vitest run (1,660 passed / 2 failed / 30 skipped), five parallel code-reading passes over all of `src/`. All P0 claims were independently spot-verified in source before filing.
**Operator intent:** ONE stub tracking all three tiers; promote via `/create-workorder --from-stub=STUB-W45PTN` as a **strict phased** (rolling multi-phase) workorder. Natural phase boundaries: P0 → P1 → P2. P0 items are independent of each other and can be sub-sequenced freely; P1 item 7 (legacy retirement) should precede or absorb P1 items that touch the same files.

---

## P0 — Correctness & trust (target: days)

| # | Item | Evidence (file:line) | Notes |
|---|------|----------------------|-------|
| P0-1 | Fix `coderef-query`: it never calls `analyzer.analyze()` before `executor.execute()`, so EVERY invocation throws "No graph available" and exits 1; errored results are then cached 5 min | `src/cli/coderef-query.ts:86-95`; throw at `src/analyzer/analyzer-service.ts:147-150`; error-caching at `src/query/query-executor.ts:137-141` | One-line fix + stop caching errored results. NOTE: interacts with P1-7 — if legacy stack is retired soon, consider fixing minimally or retiring the bin in the same phase |
| P0-2 | Gate or remove `coderef-analyze --type=breaking-changes`: all three diff/signature extractors are placeholder stubs returning empty (`getChangedElements` → `[]`, `extractSignaturesFromRef`/`FromWorktree` → empty Maps), so users get confident empty reports | `src/context/breaking-change-detector/diff-analyzer.ts:130-160`; wired at `src/cli/coderef-analyze.ts:21`; 29-case test suite mocks past the stubs (`src/context/__tests__/breaking-change-detector.test.ts:30-39`) | Minimum: hard-error or "not implemented" banner. Implementing for real is NOT P0 scope |
| P0-3a | RAG incremental: failed chunks are persisted as indexed — `updateState(chunksToIndex)` records the full to-index list including embedding FAILURES, so failures are never retried until file change or `--force` | `src/integration/rag/indexing-orchestrator.ts:919` | Persist state only for successfully embedded+upserted chunks |
| P0-3b | RAG incremental: `chunksToDelete` is computed but never wired to `vectorStore.delete()` — vectors for deleted/renamed files stay queryable forever | computed at `src/integration/rag/incremental-indexer.ts:232,245`; discarded by `filterChangedChunks`; no delete call in orchestrator | Wire deletion into the orchestrator run |
| P0-4a | Add `.coderef/**` to scanner `DEFAULT_EXCLUDE_PATTERNS` — the tool currently re-ingests its own artifacts on rescan (explains prior "ghost index entries types.d.ts/scanner.js" from MCP dogfood) | `src/scanner/scanner.ts:297-316` | Also check pipeline ignore-rules for parity (`src/pipeline/ignore-rules.ts`) |
| P0-4b | `git rm` committed self-scan residue inside src/: `src/.coderef/` (INDEX.md, graph.json, index.json, +RESOURCE-SHEET.md), `src/scanner/.coderef/incremental-cache.json` (machine-specific absolute paths, git-tracked), `src/integration/llm/.coderef/` (rag-index.json git-tracked; includes `rag-vectors.sqlite/` DIRECTORY — residue of path bug introduced 9da3ac2, fixed 4ac8aef), stray `.d.ts.map` files without siblings (`src/parser/parser.d.ts.map`, `src/export/*.d.ts.map`, `src/integration/**/*.d.ts.map`, `src/index.d.ts.map`) | `git ls-files` confirms tracking; `.gitignore:34` has `.coderef/` but these predate/bypass it | On-disk-only residue (`src/cli/.coderef/` ~11MB, untracked) can just be deleted locally |
| P0-5 | Replace hardcoded dev-machine absolute paths in shipped bins with env/config resolution | `src/cli/coderef-watch.ts:233-234` (log-session-event.mjs default), `:274-277` (dist fallback); `src/cli/coderef-rag-server.ts:386-389` (same pattern) | Breaks any non-willh machine; silently no-ops or fails over |
| P0-6 | Fix the 2 failing tests to restore green baseline: (a) `buildDependencyGraph.test.ts` ENOENT — fixture `.test-project` parent dir not created before mkdir of `.coderef`; (b) scanner recursive-mode test failure (`scanCurrentElements(__dirname, 'ts', {recursive:false})` area) | `src/fileGeneration/__tests__/buildDependencyGraph.test.ts:126` → `src/fileGeneration/buildDependencyGraph.ts:151`; second failure in scanner suite (vitest tail, run 2026-07-02 02:43) | Suite otherwise: 1,660 pass / 30 skip / 156 files / 32.6s |

## P1 — Structural (target: weeks)

| # | Item | Evidence (file:line) | Notes |
|---|------|----------------------|-------|
| P1-7 | Execute already-scheduled legacy-stack retirement (DR-PHASE-5-C): port `coderef-analyze` onto canonical `.coderef/graph.json`; delete `src/analyzer/graph-builder.ts` (marked `@deprecated`, `:11-13`), `src/analyzer/graph-analyzer.ts`, `src/query/query-executor.ts` + `coderef-query` bin (or reimplement on canonical graph) | Edge-vocabulary mismatch: legacy filters plural `'calls'/'imports'` (`graph-analyzer.ts:61,81`; `multi-hop-traversal.ts:264`) vs canonical singular `'call'/'import'` (`src/pipeline/graph-builder.ts:83`) — silent empty results. Inverted semantics: `'what-calls-me'` returns callees; `'what-depends-on'` pair swapped (`query-executor.ts:160-178`). Unsound recursive cycle detection (`graph-analyzer.ts:252-305`, fake node `{id} as any` at `:268`) | Deletion resolves P0-1 permanently, plus the inverted-semantics and vocabulary bugs wholesale. Prior memory: STUB-9F63EJ retire-ruling was sequenced after TOOL-HOTSPOTS — check for supersession/overlap before filing new work |
| P1-8 | Fix TS relationship extraction in Scanner path: `JSCallDetector` parses with plain Acorn (no TS plugin) so any TS-syntax file silently yields empty `calls[]`/`imports[]`; `.jsx` never wired at all | `src/analyzer/js-parser.ts:21` (plain acorn import); null-return `:105-121`; silent `[]` at `src/analyzer/js-call-detector/index.ts:106-109,150-153`; wired for 'ts' at `src/scanner/scanner.ts:1146,1268-1299`; dead TS-node visitor branches (`visitor.ts:159-177` — Acorn can never emit them) | Route through tree-sitter facts (as PipelineOrchestrator does) or the TS-API `CallDetector`. MUST add test: non-empty `calls[]`/`imports[]` for a real .ts fixture — currently zero coverage of this hole. Canonical populate pipeline is NOT affected |
| P1-9 | Type-system hardening: enable `noEmitOnError` immediately; migrate `strict`/`noImplicitAny` per directory (pipeline first — closest to clean) | `tsconfig.json:11-13` (`noImplicitAny:false, strict:false, noEmitOnError:false`); 224 `: any` / 30 `as any` / 51 `Record<string,any>` in non-test src; tsconfig excludes `src/cli` + `src/integration` from base build (`:30-31`), re-included via `tsconfig.cli.json` | Branded path types exist only in `src/integration/rag/path-types.ts` — consider widening |
| P1-10 | Extract ONE shared LLM-provider/vector-store factory — currently 4 copies that must be kept in sync | verbatim copies: `src/cli/rag-index.ts:269-371`, `src/cli/rag-search.ts:264-361`; re-implemented inline: `src/cli/coderef-mcp-server.ts:780-801`, `src/cli/rag-eval.ts:153-176`; rag-server hardcodes own dim table (`coderef-rag-server.ts:240`) instead of MODEL_REGISTRY | Single module consuming `src/integration/llm/model-registry.ts` |
| P1-11 | Fix or drop `coderef-search`: requires `.coderef/search-index.json` which NOTHING generates; error text cites nonexistent bin name "coderef-populate" | `src/cli/coderef-search.ts:59` (requirement), `:33,67-69` (wrong name) | Either add a search-index generator to populate, or delete the bin |
| P1-12 | Centralize path normalization: 60+ hand-rolled `.replace(/\\/g,'/')` sites across 25+ files, plus 2 alternate idioms; `EntityRegistry` UUIDs key on UNNORMALIZED `file:name:line` so Windows-vs-posix spelling yields different identities for the same element (zero tests on entity-registry) | utility exists: `src/utils/coderef-id.ts:36-42` (`normalizeProjectPath`); scattered sites: `scanner.ts:667,855`, `coderef-mcp-server.ts:196-197,268,592,677,694`, `context-generator.ts` (10 sites), `pattern-generator.ts` (9), `ignore-rules.ts:42,87-88`, `fuzzy-resolver.ts:226-227`, `indexing-orchestrator.ts:59-62`, `projections.ts:88`, `import-resolver.ts:725`; UUID gap: `src/registry/entity-registry.ts:31-34` vs `coderef-id.ts:55` | Route all through one utility; add entity-registry normalization test |

## P2 — Perf & hygiene (opportunistic)

| # | Item | Evidence (file:line) | Notes |
|---|------|----------------------|-------|
| P2-13 | Scan performance: skip regex pass when tree-sitter succeeded (currently ALWAYS runs unless fallback explicitly disabled); parse once per engine (detectImports+detectCalls each independently re-parse via Acorn; fresh `new JSCallDetector()` per file defeats caches); go async (sync `readFileSync` everywhere); fix O(lines²) comment detection; fix per-match full-file Babel re-parse in frontend-call extraction | regex always-on: `scanner.ts:1123,1183-1213,1339-1356`; double Acorn parse: `js-call-detector/index.ts:106,150`, fresh instance `scanner.ts:1149,1264`; sync IO: `scanner.ts:1104`, `tree-sitter-scanner.ts:189`, `js-parser.ts:133`; O(n²): `scanner.ts:444,1485-1553,1560-1586`; Babel-per-match: `scanner.ts:88-120` | Measured baseline: 71.9s / 343 files stats-only (2026-07-02). Target: single-digit seconds on this repo |
| P2-14 | Delete dead code: `src/pipeline/incremental-cache.ts` (298-line stale copy of `src/cache/incremental-cache.ts`, zero importers); `src/generator/generateFrontendCalls.ts` (zero importers; frontend-calls generation exists 3×); dead LLM enricher (`src/semantic/llm-enricher.ts:59-65` unconditionally self-disables; `populate --llm-enrich` parsed no-op at `populate.ts:234`); dead `--plugins`/`--no-plugins` flags (`scan.ts:157-166`, never consumed); orphaned context files (`task-context-generator.ts`, `agentic-formatter.ts`, `edge-case-detector.ts`, `example-extractor.ts` — not exported, no consumers); dead Scanner route methods (`scanner.ts:398-434,472-487,493-522,528-543,549-578`); orphaned `src/errors/` class hierarchy (no production importers) + unused `src/scanner/error-reporter.ts` | see refs inline | Also: ~10 inline structural re-declarations of `DependencyGraph` should collapse to one import |
| P2-15 | Plugin system: wire `src/plugins/` (registry + 3 loaders + manifest validation, all complete) into the scanner, or move out of src until real. `--plugins` filter is also broken by construction (`getDetectorPluginName` expects `plugin/detector` prefix never set — `plugin-scanner.ts:201-207` vs `npm-loader.ts:180-182`) | zero importers outside `src/plugins/`; not exported from `src/index.ts` | Decision needed at WO time: wire vs extract |
| P2-16 | Vector store: adopt sqlite-vec (or LanceDB), or minimum honest-rename + atomic temp+rename writes. Current "sqlite" store is pretty-printed JSON, full rewrite on every upsert (non-atomic, corruption risk), O(n) brute-force cosine scan | `src/integration/vector/sqlite-store.ts:9-21` (admission), `:237-248` (rewrite), `:330-366` (scan) | Pairs with P0-3 (incremental correctness) for large-repo continuous indexing |
| P2-17 | Test hygiene: un-skip the fully-mocked `ollama-provider-unreachable` suite (`describe.skip` at `:22`, WO-FAILING-TESTS-TRIAGE-001); include `.test.mjs`/`.test.py` in vitest include or convert (6 mjs + 1 py currently never run — `vitest.config.ts:6`); dead determinism-mask field `processingTimeMs` in indexing-gate test (`:73`) | `src/integration/llm/__tests__/ollama-provider-unreachable.test.ts:22` | Zero-test modules worth backfilling opportunistically: registry, adapter, config/presets, cache, query, utils/logger |
| P2-18 | CLI consistency: one shared arg/exit-code helper (3 parsing styles, divergent unknown-flag behavior, divergent exit codes); fix `rag-search --flag=value` parsing (`--top-k=5` silently swallows next arg — `rag-search.ts:110-118` vs `:143-164`); NaN-unchecked parseInt (`:145,188`); `scan.ts` positional-first requirement undocumented (`scan.ts:148`) | see refs inline | Low risk, high polish |

## Constraints / cautions for WO planning

- **Do not regress the canonical pipeline.** `src/pipeline` resolvers + Phase-6 validator + MCP server are the healthy core (66 invariant-named pipeline test files, frozen dual-ac fixture with mutation-guard evidence). All P1-7 deletion work must keep `.coderef/graph.json` schema and MCP tool behavior byte-stable (locked 14-field validation report; additive-only).
- **Check STUB-9F63EJ** (existing retire-ruling, sequenced after TOOL-HOTSPOTS per project memory) for overlap with P1-7 before minting duplicate work — supersession may apply.
- **Windows-first environment**: path normalization changes (P1-12) must be tested against the Windows-vs-posix UUID identity gap and the known Windows copy-mtime skew (see `indexing-gate-invariant.test.ts:148-153` precedent).
- Layers.json lives in sibling ASSISTANT repo (not bundled) — populate resolution is cwd-independent since ed5d7e3; don't reintroduce cwd assumptions when touching CLI path code.
- The 30 skipped tests include intentional skips; only the ollama-unreachable suite is flagged for un-skip (it is fully mock-based).
